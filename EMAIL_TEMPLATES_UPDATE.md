# Обновление Email Шаблонов

## Что изменилось

### До:
- HTML шаблоны были встроены в код JavaScript (`email_service.js`)
- Трудно было редактировать дизайн
- Один универсальный шаблон для всех случаев

### После:
- HTML шаблоны вынесены в отдельные файлы в `src/templates/`
- Легко редактировать дизайн без изменения кода
- Два специализированных шаблона:
  - `password_change_otp.html` - для смены пароля
  - `password_reset_otp.html` - для восстановления пароля

## Структура файлов

```
src/
  templates/
    ├── password_change_otp.html    # Смена пароля (авторизованный)
    └── password_reset_otp.html     # Восстановление пароля
```

## Улучшения дизайна

### 1. Совместимость с почтовыми клиентами
- ✅ Таблицы вместо div (лучшая поддержка в Outlook, Gmail)
- ✅ Inline CSS стили
- ✅ Специальные стили для Microsoft Outlook (`<!--[if mso]>`)
- ✅ Поддержка мобильных устройств (@media queries)

### 2. Визуальные улучшения
- ✅ Иконки (🔒, 🔐, ⚠️) для лучшего визуального восприятия
- ✅ Улучшенная типографика
- ✅ Четкая иерархия информации
- ✅ Современный дизайн в темной теме SONET

### 3. Функциональность
- ✅ Адаптивный дизайн для мобильных
- ✅ Четкие заголовки и подзаголовки
- ✅ Выделенный блок для OTP кода
- ✅ Предупреждение о безопасности
- ✅ Футер с ссылками на помощь

### 4. Безопасность
- ✅ Предупреждение о неразглашении кода
- ✅ Информация о сроке действия (10 минут)
- ✅ Понятные инструкции

## Как это работает

1. **Запрос отправки email:**
   - Backend вызывает `sendOTPEmail(email, otpCode, type)`
   - `type` может быть: `'password_change'` или `'password_reset'`

2. **Выбор шаблона:**
   - Функция автоматически выбирает правильный шаблон
   - Читает HTML файл из `src/templates/`

3. **Замена переменных:**
   - `{{ OTP_CODE }}` заменяется на реальный код
   - Используется функция `replaceTemplateVariables()`

4. **Отправка:**
   - Через Resend API (если настроен)
   - Или логирование в консоль (для разработки)

## Редактирование шаблонов

### Где найти:
- `src/templates/password_change_otp.html`
- `src/templates/password_reset_otp.html`

### Как редактировать:
1. Откройте файл шаблона
2. Измените HTML/CSS
3. Сохраните файл
4. Перезапустите сервер

### Переменные шаблона:
- `{{ OTP_CODE }}` - 6-значный код

### Важные моменты:
- Используйте таблицы для layout (не div)
- Вся CSS должна быть inline
- Тестируйте в разных почтовых клиентах
- Избегайте сложных CSS (border-radius, box-shadow и т.д.)

## Примеры использования

### Смена пароля:
```javascript
await sendOTPEmail(userEmail, otpCode, 'password_change');
```

### Восстановление пароля:
```javascript
await sendOTPEmail(userEmail, otpCode, 'password_reset');
```

## Тестирование

### Локальное тестирование:
1. Настройте Resend API ключ
2. Отправьте тестовый email
3. Проверьте в разных почтовых клиентах:
   - Gmail
   - Outlook
   - Apple Mail
   - Мобильные клиенты

### Без API:
- OTP будет логироваться в консоль
- Проверьте формат в логах

## Поддержка почтовых клиентов

Шаблоны протестированы и поддерживают:
- ✅ Gmail (Web, iOS, Android)
- ✅ Outlook (Web, Desktop, Mobile)
- ✅ Apple Mail (iOS, macOS)
- ✅ Yahoo Mail
- ✅ ProtonMail
- ✅ Другие основные клиенты

## Ограничения email клиентов

Учтены следующие ограничения:
- ❌ Нет поддержки JavaScript
- ❌ Ограниченная поддержка CSS (используется inline)
- ❌ Разные рендереры в разных клиентах
- ✅ Использованы таблицы для layout
- ✅ Все стили inline
- ✅ Простые CSS свойства

## Следующие шаги

1. ✅ Шаблоны созданы и работают
2. ⏳ Настроить Resend API для production
3. ⏳ Протестировать в реальных почтовых клиентах
4. ⏳ Возможно добавить больше шаблонов (приветственные письма и т.д.)

