# 🚀 Предложения по улучшению функций постов

## 📊 Анализ текущей функциональности

### ✅ Что уже реализовано:
- ✅ Создание постов (изображения и видео)
- ✅ Лайки постов
- ✅ Комментарии с ответами
- ✅ Сохранение постов (bookmark)
- ✅ Редактирование постов
- ✅ Удаление постов
- ✅ Хештеги и упоминания
- ✅ Геолокация для постов
- ✅ Видимость постов (public, friends, private)
- ✅ Время жизни постов (expiresAt)
- ✅ Поделиться постами
- ✅ Лента постов (feed)
- ✅ Видео посты (Shorts)

---

## 🎯 Предложения улучшений (можно добавить сейчас)

### 1. 👁️ **Счетчик просмотров постов** ⭐⭐⭐
**Приоритет: ВЫСОКИЙ**

**Что добавить:**
- Отслеживание просмотров каждого поста
- Отображение количества просмотров под постом
- Статистика просмотров для автора поста

**Реализация:**
```dart
// В модели Post добавить:
final int viewsCount;
final bool isViewed; // просмотрел ли текущий пользователь

// В PostCard добавить отображение:
if (post.viewsCount > 0)
  Text('${post.viewsCount} просмотров', style: ...)
```

**Backend:**
- Таблица `post_views (user_id, post_id, viewed_at)`
- Endpoint: `POST /api/posts/:id/view` - засчитать просмотр
- Автоматически засчитывать просмотр при открытии поста

**Сложность:** 🟢 Низкая (1-2 дня)

---

### 2. 🔄 **Репосты (Reposts/Reshare)** ⭐⭐⭐
**Приоритет: ВЫСОКИЙ**

**Что добавить:**
- Кнопка "Репост" в меню поста
- Возможность добавить свой комментарий к репосту
- Отображение репостов в ленте с указанием оригинального автора
- Счетчик репостов

**UI:**
```
[Иконка репоста] [Счетчик] | [Лайк] [Комментарий] [Поделиться]
```

**Реализация:**
```dart
// В модели Post добавить:
final int repostsCount;
final bool isReposted;
final Post? originalPost; // если это репост
final String? repostComment; // комментарий к репосту

// В PostCard добавить:
IconButton(
  icon: Icon(isReposted ? EvaIcons.repeat : EvaIcons.repeatOutline),
  onPressed: () => _toggleRepost(),
)
```

**Backend:**
- Таблица `reposts (id, user_id, original_post_id, comment, created_at)`
- Endpoints:
  - `POST /api/posts/:id/repost` - создать репост
  - `DELETE /api/posts/:id/repost` - убрать репост
  - `GET /api/posts/:id/reposts` - список репостов

**Сложность:** 🟡 Средняя (3-4 дня)

---

### 3. 📌 **Закрепление постов в профиле** ⭐⭐
**Приоритет: СРЕДНИЙ**

**Что добавить:**
- Возможность закрепить до 3 постов вверху профиля
- Закрепленные посты отображаются первыми в профиле
- Иконка булавки на закрепленных постах

**UI:**
- В меню поста (только для своих постов): "Закрепить пост"
- В профиле: закрепленные посты с иконкой 📌

**Реализация:**
```dart
// В модели Post добавить:
final bool isPinned;

// В ProfileScreen:
if (post.isPinned) 
  Icon(EvaIcons.pin, size: 16, color: Colors.blue)
```

**Backend:**
- Поле `is_pinned BOOLEAN DEFAULT FALSE` в таблице `posts`
- Endpoints:
  - `POST /api/posts/:id/pin` - закрепить
  - `DELETE /api/posts/:id/pin` - открепить
- Ограничение: максимум 3 закрепленных поста

**Сложность:** 🟢 Низкая (1-2 дня)

---

### 4. 📊 **Базовая аналитика постов** ⭐⭐⭐
**Приоритет: ВЫСОКИЙ**

**Что добавить:**
- Экран статистики поста (только для автора)
- Показывать: просмотры, лайки, комментарии, сохранения, репосты
- График активности (если есть данные)

**UI:**
- Кнопка "Статистика" в меню поста (только для автора)
- Экран `PostAnalyticsScreen` с карточками метрик

**Реализация:**
```dart
class PostAnalytics {
  final int views;
  final int likes;
  final int comments;
  final int saves;
  final int reposts;
  final int reach; // охват
  final List<AnalyticsDataPoint> activity; // график
}
```

**Backend:**
- Endpoint: `GET /api/posts/:id/analytics`
- Агрегация данных из таблиц: `post_views`, `likes`, `comments`, `saved_posts`, `reposts`

**Сложность:** 🟡 Средняя (2-3 дня)

---

### 5. 🗂️ **Архив постов** ⭐⭐
**Приоритет: СРЕДНИЙ**

**Что добавить:**
- Возможность скрыть пост из профиля без удаления
- Раздел "Архив" в профиле
- Восстановление постов из архива

**UI:**
- В меню поста: "Архивировать"
- В профиле: вкладка "Архив" (только для владельца)

**Реализация:**
```dart
// В модели Post добавить:
final bool isArchived;

// В ProfileScreen добавить вкладку:
TabBar(
  tabs: [
    Tab(text: 'Посты'),
    Tab(text: 'Архив'), // только для своего профиля
  ],
)
```

**Backend:**
- Поле `is_archived BOOLEAN DEFAULT FALSE` в таблице `posts`
- Endpoints:
  - `POST /api/posts/:id/archive` - архивировать
  - `DELETE /api/posts/:id/archive` - восстановить
- Фильтрация: не показывать архивированные посты в ленте и профиле (кроме раздела "Архив")

**Сложность:** 🟢 Низкая (1-2 дня)

---

### 6. 📸 **Карусель медиа (несколько фото/видео в одном посте)** ⭐⭐⭐
**Приоритет: ВЫСОКИЙ**

**Что добавить:**
- Возможность загрузить до 10 фото/видео в один пост
- Swipe между медиа в посте
- Индикатор текущего медиа (точки внизу)

**UI:**
- В `CreatePostScreen`: кнопка "Добавить еще" после выбора первого медиа
- В `PostCard`: PageView для переключения между медиа
- Индикатор: `1/5` в углу поста

**Реализация:**
```dart
// В модели Post изменить:
final List<String> mediaUrls; // вместо mediaUrl
final List<String>? thumbnailUrls;
final int currentMediaIndex; // для отображения

// В PostCard:
PageView.builder(
  itemCount: post.mediaUrls.length,
  itemBuilder: (context, index) => _buildMedia(post.mediaUrls[index]),
)
```

**Backend:**
- Изменить `media_url TEXT` на `media_urls JSONB` или отдельная таблица `post_media`
- Endpoint создания поста: принимать массив файлов

**Сложность:** 🔴 Высокая (5-7 дней)

---

### 7. 🎯 **Опросы в постах (Polls)** ⭐⭐
**Приоритет: СРЕДНИЙ**

**Что добавить:**
- Создание опроса с 2-4 вариантами ответа
- Голосование в опросах
- Отображение результатов (проценты)
- Ограничение по времени опроса (опционально)

**UI:**
- В `CreatePostScreen`: переключатель "Опрос" вместо медиа
- Отображение опроса в `PostCard` с кнопками выбора
- После голосования показывать результаты

**Реализация:**
```dart
class Poll {
  final String question;
  final List<PollOption> options;
  final int? expiresAt; // timestamp
  final bool hasVoted;
  final String? votedOptionId;
}

class PollOption {
  final String id;
  final String text;
  final int votesCount;
  final double percentage;
}
```

**Backend:**
- Таблицы: `polls`, `poll_options`, `poll_votes`
- Endpoints:
  - `POST /api/posts/:id/poll/vote` - проголосовать
  - `GET /api/posts/:id/poll` - получить результаты

**Сложность:** 🟡 Средняя (3-4 дня)

---

### 8. 🏷️ **Теги людей на фото/видео** ⭐⭐
**Приоритет: СРЕДНИЙ**

**Что добавить:**
- Отметка пользователей на фото/видео
- Позиция тега (координаты x, y)
- Уведомления отмеченным пользователям
- Отображение тегов при просмотре поста

**UI:**
- При создании поста: кнопка "Отметить людей"
- Tap на фото в посте: показать теги
- В профиле: вкладка "Отметки" (посты, где пользователь отмечен)

**Реализация:**
```dart
class PostTag {
  final String userId;
  final String username;
  final double x; // 0.0 - 1.0
  final double y; // 0.0 - 1.0
}

// В модели Post:
final List<PostTag> tags;
```

**Backend:**
- Таблица `post_tags (id, post_id, user_id, x, y)`
- Endpoints:
  - `POST /api/posts/:id/tags` - добавить тег
  - `DELETE /api/posts/:id/tags/:tagId` - удалить тег

**Сложность:** 🟡 Средняя (3-4 дня)

---

### 9. 💬 **Черновики постов** ⭐
**Приоритет: НИЗКИЙ**

**Что добавить:**
- Сохранение незавершенных постов как черновиков
- Список черновиков в профиле
- Продолжение редактирования черновика

**UI:**
- В `CreatePostScreen`: кнопка "Сохранить как черновик"
- В профиле: раздел "Черновики"

**Реализация:**
```dart
// В модели Post добавить:
final bool isDraft;

// Endpoint:
GET /api/posts/drafts - получить черновики
```

**Backend:**
- Поле `is_draft BOOLEAN DEFAULT FALSE` в таблице `posts`
- Фильтрация: черновики не показываются в ленте

**Сложность:** 🟢 Низкая (1 день)

---

### 10. ⏰ **Планирование постов (Scheduled Posts)** ⭐⭐
**Приоритет: СРЕДНИЙ**

**Что добавить:**
- Выбор даты и времени публикации
- Список запланированных постов
- Автоматическая публикация по расписанию

**UI:**
- В `CreatePostScreen`: переключатель "Опубликовать сейчас" / "Запланировать"
- Выбор даты и времени
- В профиле: раздел "Запланированные"

**Реализация:**
```dart
// В модели Post:
final DateTime? scheduledAt;

// Backend cron job для публикации
```

**Backend:**
- Поле `scheduled_at TIMESTAMPTZ` в таблице `posts`
- Cron job для проверки и публикации запланированных постов
- Endpoint: `GET /api/posts/scheduled`

**Сложность:** 🟡 Средняя (3-4 дня)

---

## 📋 Рекомендуемый порядок реализации

### 🔴 **Фаза 1 (1-2 недели) - Быстрые улучшения:**
1. ✅ Счетчик просмотров (1-2 дня)
2. ✅ Закрепление постов (1-2 дня)
3. ✅ Архив постов (1-2 дня)
4. ✅ Черновики постов (1 день)

### 🟠 **Фаза 2 (2-3 недели) - Важные функции:**
5. ✅ Репосты (3-4 дня)
6. ✅ Базовая аналитика (2-3 дня)
7. ✅ Планирование постов (3-4 дня)

### 🟡 **Фаза 3 (1 месяц) - Продвинутые функции:**
8. ✅ Карусель медиа (5-7 дней)
9. ✅ Опросы (3-4 дня)
10. ✅ Теги людей (3-4 дня)

---

## 💡 Дополнительные идеи (на будущее)

- **Коллекции постов** - группировка постов в альбомы/коллекции
- **Музыка в постах** - добавление аудио к постам
- **Стикеры и эмодзи на медиа** - редактирование фото/видео
- **Фильтры для фото** - применение фильтров перед публикацией
- **Сортировка постов** - по новизне, популярности, лайкам
- **Фильтры в ленте** - показывать только фото/только видео
- **История изменений поста** - показывать что было изменено

---

## 🎯 Итоговые рекомендации

**Начать с:**
1. **Счетчик просмотров** - быстро реализовать, большой эффект
2. **Репосты** - очень популярная функция, повышает engagement
3. **Закрепление постов** - полезно для пользователей, легко реализовать
4. **Архив постов** - дает больше контроля пользователям

Эти 4 функции можно реализовать за 1-2 недели и они значительно улучшат пользовательский опыт!

