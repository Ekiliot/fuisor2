# 🎨 Дизайн-система приложения Fuisor

## 📋 Обзор

Приложение использует **минималистичный темный дизайн в стиле Instagram** с акцентом на чистоту, контрастность и интуитивность интерфейса.

---

## 🎨 Цветовая палитра

### Основные цвета

```dart
// Фон и поверхности
background: Color(0xFF000000)      // Чистый черный фон
surface: Color(0xFF121212)         // Темно-серый для полей ввода и карточек
border: Color(0xFF262626)          // Границы и разделители

// Текст
textPrimary: Colors.white          // Основной текст
textSecondary: Color(0xFF8E8E8E)   // Вторичный текст (подписи, метаданные)

// Акцентные цвета (Instagram-стиль)
instagramBlue: Color(0xFF0095F6)   // Основной акцент (кнопки, ссылки, фокус)
instagramPurple: Color(0xFF833AB4) // Градиент Instagram
instagramPink: Color(0xFFE1306C)    // Градиент Instagram
instagramRed: Color(0xFFFD1D1D)     // Ошибки, удаление
```

### Использование цветов

- **Черный фон (`#000000`)** — основной фон всех экранов
- **Темно-серый (`#121212`)** — поля ввода, карточки, модальные окна
- **Серый (`#262626`)** — границы, разделители, неактивные элементы
- **Белый (`#FFFFFF`)** — основной текст, иконки, активные элементы
- **Серый (`#8E8E8E`)** — вторичный текст, подписи, метаданные
- **Синий Instagram (`#0095F6`)** — кнопки, ссылки, активные состояния, фокус

---

## 📝 Типографика

### Размеры шрифтов

```dart
// Заголовки
headlineLarge: 28px, bold          // Большие заголовки
headlineMedium: 24px, w600         // Средние заголовки
headlineSmall: 20px, w600          // Малые заголовки, AppBar

// Основной текст
bodyLarge: 16px, normal            // Основной текст
bodyMedium: 14px, normal           // Вторичный текст
bodySmall: 12px, normal            // Мелкий текст, подписи
```

### Начертания

- **Bold (700)** — заголовки
- **Semi-bold (600)** — подзаголовки, AppBar
- **Normal (400)** — основной текст

### Цвета текста

- **Белый** — основной текст на черном фоне
- **Серый (`#8E8E8E`)** — вторичный текст, метаданные, подписи

---

## 🔲 Компоненты

### Кнопки

#### Elevated Button (Основная кнопка)
```dart
backgroundColor: Color(0xFF0095F6)  // Instagram синий
foregroundColor: Colors.white
borderRadius: 12px
elevation: 0                         // Без тени
padding: vertical: 16px
```

#### Text Button (Текстовая кнопка)
```dart
foregroundColor: Color(0xFF0095F6)  // Instagram синий
borderRadius: 12px
```

### Поля ввода

```dart
borderRadius: 12px
fillColor: Color(0xFF121212)        // Темно-серый фон
borderColor: Color(0xFF262626)      // Серые границы
focusedBorderColor: Color(0xFF0095F6) // Синий при фокусе
contentPadding: horizontal: 16px, vertical: 16px
```

### Карточки

```dart
// Post Card
borderRadius: 20px                  // Больше закругление для постов
backgroundColor: Color(0xFF1A1A1A)  // Темно-серый
margin: horizontal: 12px, vertical: 8px
boxShadow: [
  BoxShadow(
    color: Colors.black.withOpacity(0.3),
    blurRadius: 10,
    spreadRadius: 0,
    offset: Offset(0, 4),
  ),
]

// Обычные карточки
borderRadius: 16px
backgroundColor: Color(0xFF000000)  // Черный
elevation: 0                        // Без тени
```

### Аватары

```dart
// Стандартный аватар
radius: 20px                        // По умолчанию
backgroundColor: Color(0xFF262626)  // Темно-серый фон
fallbackIcon: EvaIcons.personOutline
iconColor: Colors.white

// Размеры аватаров
small: radius: 18px                 // В постах
medium: radius: 20px                // Стандартный
large: radius: 40px                 // В профиле
```

### Иконки

#### Библиотека
**Eva Icons** — основная библиотека иконок (вместо Material Icons)

#### Размеры
```dart
// Навигация
size: 28px                          // Увеличенные для лучшей видимости

// Действия в постах
size: 28px                          // Лайки, комментарии, сохранения

// Обычные иконки
size: 24px                          // Стандартный размер
size: 20px                          // Малые иконки (меню, опции)
```

#### Цвета
- **Белый** — активные иконки
- **Серый (`#8E8E8E`)** — неактивные иконки в навигации
- **Синий (`#0095F6`)** — активные иконки в навигации

---

## 📐 Отступы и размеры

### Отступы (Spacing)

```dart
// Маленькие отступы
4px, 8px                            // Между мелкими элементами

// Средние отступы
12px, 16px                          // Стандартные отступы
padding: EdgeInsets.symmetric(horizontal: 16, vertical: 12)

// Большие отступы
20px, 24px                          // Между секциями
```

### Размеры компонентов

```dart
// Высота кнопок
buttonHeight: 48px                  // С padding: 16px

// Высота полей ввода
inputHeight: 48px                   // С padding: 16px

// Минимальная высота касаемых элементов
touchTarget: 48px                   // Минимум для удобства
```

---

## 🔄 Закругления (Border Radius)

```dart
// Маленькие закругления
8px                                 // Мелкие элементы

// Средние закругления
12px                                // Кнопки, поля ввода, аватары
borderRadius: BorderRadius.circular(12)

// Большие закругления
16px                                // Карточки
20px                                // Post Card
borderRadius: BorderRadius.circular(20)
```

---

## 🎭 Анимации

### Длительность

```dart
// Быстрые анимации
Duration(milliseconds: 200)          // Переходы состояний

// Средние анимации
Duration(milliseconds: 300)         // Стандартные переходы

// Медленные анимации
Duration(milliseconds: 500)         // Сложные переходы
```

### Кривые анимации

```dart
// Стандартные
Curves.easeInOut                    // Плавные переходы
Curves.elasticOut                   // Эластичные эффекты (лайки)

// Специальные
Curves.fastOutSlowIn                // Быстрый старт, медленный конец
```

### Примеры анимаций

```dart
// Анимация лайка
AnimationController(
  duration: Duration(milliseconds: 300),
)
Tween<double>(begin: 1.0, end: 1.3)
CurvedAnimation(curve: Curves.elasticOut)

// Анимация счетчиков
ScaleAnimation для плавного изменения счетчиков лайков/комментариев
```

---

## 🎯 Навигация

### Bottom Navigation Bar

```dart
backgroundColor: Color(0xFF000000)  // Черный фон
selectedItemColor: Colors.white     // Активный элемент
unselectedItemColor: Color(0xFF8E8E8E) // Неактивный элемент
type: BottomNavigationBarType.fixed
elevation: 0                         // Без тени
iconSize: 28px                       // Увеличенные иконки
// Без лейблов (только иконки)
```

### App Bar

```dart
backgroundColor: Color(0xFF000000)  // Черный фон
foregroundColor: Colors.white       // Белый текст и иконки
elevation: 0                         // Без тени
titleTextStyle: 20px, w600          // Полужирный заголовок
iconSize: 28px                       // Увеличенные иконки
```

---

## 📱 Адаптивность

### Принципы

- **Минималистичный дизайн** — убраны лишние элементы Material You
- **Чистый черный фон** — максимальный контраст
- **Увеличенные иконки** — лучшая видимость (28px вместо 24px)
- **Больше закругления** — радиус 12-16px вместо 8px
- **Без теней** — elevation: 0 для плоского дизайна

---

## 🎨 Стиль компонентов

### Post Card

```dart
// Контейнер
margin: EdgeInsets.symmetric(horizontal: 12, vertical: 8)
borderRadius: 20px
backgroundColor: Color(0xFF1A1A1A)
boxShadow: мягкие тени для глубины

// Заголовок поста
padding: EdgeInsets.symmetric(horizontal: 16, vertical: 12)
avatar: 48x48px, borderRadius: 12px

// Медиа
width: 100% (full width)
aspectRatio: сохраняется оригинальное соотношение

// Действия
iconSize: 28px
spacing: 20px между иконками
```

### Profile Screen

```dart
// Аватар профиля
radius: 40px                         // Большой аватар
backgroundColor: Color(0xFF262626)

// Статистика
textStyle: bodyMedium (14px)
color: Colors.white

// Кнопки
"Edit Profile": instagramBlue
"Logout": Colors.red
```

### Stories Widget

```dart
// Аватар истории
radius: 32px
border: градиент Instagram (синий-фиолетовый-розовый)
backgroundColor: Color(0xFF262626)

// Текст имени
color: Colors.white
fontSize: 12px
```

---

## 🔍 Состояния компонентов

### Кнопки

- **Default** — синий фон (`#0095F6`), белый текст
- **Pressed** — затемнение (opacity: 0.8)
- **Disabled** — серый фон, серый текст

### Поля ввода

- **Default** — серые границы (`#262626`), темно-серый фон (`#121212`)
- **Focused** — синие границы (`#0095F6`)
- **Error** — красные границы (`#FD1D1D`)

### Иконки навигации

- **Active** — белый цвет
- **Inactive** — серый цвет (`#8E8E8E`)

---

## 📚 Рекомендации по использованию

### ✅ Что делать

1. **Использовать только темную тему** — приложение поддерживает только темный режим
2. **Следовать цветовой палитре** — использовать цвета из `AppThemes`
3. **Использовать Eva Icons** — вместо Material Icons
4. **Соблюдать отступы** — использовать стандартные значения (12px, 16px, 20px)
5. **Закругления** — 12px для кнопок/полей, 16-20px для карточек
6. **Без теней** — elevation: 0 для плоского дизайна

### ❌ Чего избегать

1. **Не использовать светлую тему** — только темный режим
2. **Не использовать Material You элементы** — минималистичный дизайн
3. **Не использовать маленькие иконки** — минимум 24px, предпочтительно 28px
4. **Не использовать тени** — elevation: 0
5. **Не использовать маленькие закругления** — минимум 12px

---

## 📖 Примеры использования

### Кнопка

```dart
ElevatedButton(
  onPressed: () {},
  child: Text('Button'),
  // Использует стили из AppThemes.darkTheme
)
```

### Поле ввода

```dart
TextField(
  decoration: InputDecoration(
    hintText: 'Enter text',
    // Использует стили из AppThemes.darkTheme
  ),
)
```

### Аватар

```dart
SafeAvatar(
  imageUrl: user.avatarUrl,
  radius: 20,
  backgroundColor: Color(0xFF262626),
  fallbackIcon: EvaIcons.personOutline,
)
```

### Иконка

```dart
Icon(
  EvaIcons.home,
  size: 28,
  color: Colors.white,
)
```

---

## 🎯 Ключевые принципы дизайна

1. **Минимализм** — убраны лишние элементы, фокус на контенте
2. **Контрастность** — белый текст на черном фоне для максимальной читаемости
3. **Instagram-стиль** — знакомый UX, интуитивная навигация
4. **Плоский дизайн** — без теней, чистые линии
5. **Увеличенные элементы** — иконки 28px для лучшей видимости
6. **Плавные анимации** — 300ms для стандартных переходов

---

## 📝 Примечания

- Все цвета определены в `lib/utils/themes.dart`
- Тема применяется глобально через `MaterialApp`
- Компоненты используют стили из темы автоматически
- Для кастомных компонентов используйте цвета из `AppThemes`
