# 🚀 Деплой API Fuisor

Руководство по деплою Node.js/Express API на различные платформы.

## 📋 Требования

- Node.js приложение (Express)
- Переменные окружения (Supabase credentials)
- Порт: `process.env.PORT` (платформы автоматически устанавливают)

## 🌐 Варианты деплоя

### 1. **Vercel** (Рекомендуется) ⭐

**Плюсы:**
- ✅ Бесплатный тариф
- ✅ Автоматический деплой из GitHub
- ✅ Отличная поддержка Node.js
- ✅ Быстрый и надежный
- ✅ Автоматический HTTPS

**Шаги:**
1. Зарегистрируйтесь на [vercel.com](https://vercel.com)
2. Подключите GitHub репозиторий
3. Настройте переменные окружения в Vercel Dashboard:
   - `SUPABASE_URL`
   - `SUPABASE_SERVICE_ROLE_KEY`
   - `JWT_SECRET` (если используется)
   - `PORT` (устанавливается автоматически)
4. Деплой произойдет автоматически

**Файл конфигурации:** `vercel.json` (уже создан)

---

### 2. **Railway** ⭐

**Плюсы:**
- ✅ Бесплатный тариф ($5 кредитов в месяц)
- ✅ Очень простой деплой
- ✅ Автоматический HTTPS
- ✅ Хорошая поддержка Node.js

**Шаги:**
1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Создайте новый проект
3. Подключите GitHub репозиторий или загрузите код
4. Railway автоматически определит Node.js проект
5. Добавьте переменные окружения в настройках проекта:
   - `SUPABASE_URL`
   - `SUPABASE_SERVICE_ROLE_KEY`
   - `JWT_SECRET` (если используется)
6. Деплой произойдет автоматически

**Файл конфигурации:** `railway.json` (уже создан)

---

### 3. **Render**

**Плюсы:**
- ✅ Бесплатный тариф (с ограничениями)
- ✅ Простой деплой
- ✅ Автоматический HTTPS

**Шаги:**
1. Зарегистрируйтесь на [render.com](https://render.com)
2. Создайте новый Web Service
3. Подключите GitHub репозиторий
4. Настройки:
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
5. Добавьте переменные окружения:
   - `SUPABASE_URL`
   - `SUPABASE_SERVICE_ROLE_KEY`
   - `JWT_SECRET` (если используется)
6. Нажмите "Create Web Service"

**Файл конфигурации:** `render.yaml` (уже создан)

---

### 4. **Heroku**

**Плюсы:**
- ✅ Надежный и проверенный
- ✅ Хорошая документация
- ⚠️ Платный (от $5/месяц)

**Шаги:**
1. Установите Heroku CLI
2. Войдите: `heroku login`
3. Создайте приложение: `heroku create fuisor-api`
4. Добавьте переменные окружения:
   ```bash
   heroku config:set SUPABASE_URL=your_url
   heroku config:set SUPABASE_SERVICE_ROLE_KEY=your_key
   ```
5. Деплой: `git push heroku main`

**Файл:** Создайте `Procfile`:
```
web: node src/index.js
```

---

### 5. **DigitalOcean App Platform**

**Плюсы:**
- ✅ Хорошая производительность
- ✅ Простой деплой
- ⚠️ Платный (от $5/месяц)

**Шаги:**
1. Зарегистрируйтесь на [digitalocean.com](https://digitalocean.com)
2. Создайте новый App
3. Подключите GitHub репозиторий
4. Настройте переменные окружения
5. Деплой произойдет автоматически

---

## 🔐 Переменные окружения

Убедитесь, что на платформе деплоя установлены следующие переменные:

```env
SUPABASE_URL=your_supabase_url
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
JWT_SECRET=your_jwt_secret (если используется)
NODE_ENV=production
PORT=3000 (обычно устанавливается автоматически платформой)
```

## 📝 Важные замечания

1. **Порт:** Платформы обычно автоматически устанавливают переменную `PORT`, ваш код уже использует `process.env.PORT || 3000`

2. **CORS:** Убедитесь, что в `src/index.js` CORS настроен правильно для вашего домена Flutter приложения

3. **Supabase:** Убедитесь, что Supabase проект настроен и доступен из интернета

4. **Файлы:** Убедитесь, что `.env` файл не попадает в репозиторий (добавьте в `.gitignore`)

## 🚀 Рекомендация

Для начала рекомендую **Vercel** или **Railway**:
- Бесплатные тарифы
- Простой деплой
- Хорошая документация
- Автоматический HTTPS

После деплоя обновите `baseUrl` в Flutter приложении (`fuisor_app/lib/services/api_service.dart`) на URL вашего задеплоенного API.

## 🔄 Обновление Flutter приложения после деплоя

После успешного деплоя API:

1. Откройте `fuisor_app/lib/services/api_service.dart`
2. Найдите строку с `baseUrl` (обычно в начале класса)
3. Замените локальный URL на URL вашего задеплоенного API:
   ```dart
   // Было (локально):
   static const String baseUrl = 'http://localhost:3000/api';
   
   // Стало (после деплоя):
   static const String baseUrl = 'https://your-api.vercel.app/api'; // или ваш URL
   ```

4. Пересоберите Flutter приложение

## ⚠️ Важно для CORS

Если у вас возникнут проблемы с CORS после деплоя, обновите настройки CORS в `src/index.js`:

```javascript
app.use(cors({
  origin: [
    'http://localhost:3000', // для локальной разработки
    'https://your-flutter-app.web.app', // для веб версии
    // Добавьте другие домены по необходимости
  ],
  credentials: true
}));
```

Или для разработки можно временно разрешить все домены:
```javascript
app.use(cors()); // Разрешает все домены (только для разработки!)
```

