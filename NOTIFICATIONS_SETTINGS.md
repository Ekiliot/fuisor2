# 🔔 Настройка уведомлений

## ✅ Реализовано

Добавлена возможность отключать/включать уведомления через настройки приложения.

## 📍 Где находится

**Settings → General → Notifications**

## 🔧 Как работает

### Включение уведомлений
1. При включении переключателя:
   - Сохраняется настройка в `SharedPreferences` (`notifications_enabled`)
   - Инициализируется FCM сервис (если еще не инициализирован)
   - FCM токен отправляется на сервер
   - Уведомления начинают поступать

### Отключение уведомлений
1. При отключении переключателя:
   - Сохраняется настройка в `SharedPreferences`
   - FCM токен удаляется локально
   - FCM токен удаляется с сервера (отправляется пустая строка)
   - Уведомления перестают поступать

## 🔄 Автоматическое применение

### При запуске приложения
- Проверяется настройка `notifications_enabled`
- Если `false` - FCM не инициализируется
- Если `true` - FCM инициализируется как обычно

### При входе в аккаунт
- Проверяется настройка перед отправкой токена на сервер
- Если уведомления отключены - токен не отправляется
- Если включены, но FCM не инициализирован - инициализируется автоматически

## 📱 UI/UX

- Переключатель показывает состояние загрузки (индикатор)
- Показывается snackbar при успешном изменении
- Показывается ошибка при неудаче (с откатом изменения)

## 🗄️ Хранение

Настройка хранится в `SharedPreferences`:
- Ключ: `notifications_enabled`
- Тип: `bool`
- По умолчанию: `true` (включено)

## 🔒 Безопасность

- Токен полностью удаляется с сервера при отключении
- Локальный токен также удаляется
- При следующем включении будет создан новый токен

## 🧪 Тестирование

1. Откройте Settings → Notifications
2. Отключите переключатель
3. Проверьте, что токен удален (в логах)
4. Включите обратно
5. Проверьте, что токен отправлен на сервер

## 📝 Файлы изменены

- `fuisor_app/lib/screens/settings_screen.dart` - добавлена логика переключателя
- `fuisor_app/lib/main.dart` - проверка настройки при инициализации
- `src/routes/user.routes.js` - поддержка удаления токена (пустая строка)
