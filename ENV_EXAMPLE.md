# 🔐 Как добавить Firebase Admin Config в .env

## Важно!

Вы прислали Service Account Key для Firebase. Это **приватные ключи** - храните их в безопасности!

## Форматирование для .env

Весь JSON должен быть в **одну строку**. Используйте одинарные кавычки снаружи, чтобы не экранировать кавычки внутри JSON.

### Правильный формат:

```env
```

## Шаги:

1. **Создайте файл `.env`** в корне проекта (если его нет)

2. **Добавьте переменную** в формате выше:
   - Используйте **одинарные кавычки** `'...'` снаружи
   - Весь JSON в **одну строку**
   - Сохраните все символы `\n` в private_key как есть

3. **Проверьте**, что файл `.env` в `.gitignore` (чтобы не закоммитить приватные ключи!)

## Для Production (Vercel/Railway/Render):

Добавьте ту же переменную `FIREBASE_ADMIN_CONFIG` в настройках вашего хостинга:
- Скопируйте весь JSON (в одну строку)
- Вставьте в переменную окружения на хостинге

## ⚠️ Безопасность:

- ❌ **НЕ коммитьте** `.env` файл в Git
- ❌ **НЕ делитесь** этим ключом публично
- ✅ Используйте разные ключи для dev/prod если нужно
- ✅ Храните в безопасном месте
