# ✅ FCM Implementation Checklist

## 📱 Frontend (Flutter)

- [x] Добавлены Firebase зависимости (`firebase_core`, `firebase_messaging`)
- [x] Создан FCM сервис (`lib/services/fcm_service.dart`)
- [x] Обновлен AndroidManifest.xml для FCM
- [x] Добавлен Google Services плагин в Android
- [x] Интегрирован FCM в `main.dart`
- [x] Обновлен API сервис для отправки FCM токена
- [x] Удален старый WorkManager код

## 🔧 Frontend настройка (требуется)

- [ ] Создать Firebase проект
- [ ] Добавить Android приложение в Firebase Console
- [ ] Скачать `google-services.json` и поместить в `android/app/`
- [ ] Протестировать получение FCM токена

## 🖥️ Backend (Node.js)

- [x] Добавлен `firebase-admin` в `package.json`
- [x] Создан FCM сервис (`src/utils/fcm_service.js`)
- [x] Создана миграция для `fcm_token` в таблице `profiles`
- [x] Создан эндпоинт `PUT /api/users/fcm-token`
- [x] Интегрирована отправка FCM в `createNotification()`
- [x] Добавлена отправка FCM для сообщений
- [x] Инициализация FCM при старте сервера

## 🔧 Backend настройка (требуется)

- [ ] Установить зависимости: `npm install`
- [ ] Применить миграцию БД: `add_fcm_token_to_profiles.sql`
- [ ] Получить Service Account Key из Firebase Console
- [ ] Добавить переменную окружения `FIREBASE_ADMIN_CONFIG`
- [ ] Протестировать отправку уведомлений

## 🔔 Типы уведомлений (все покрыты)

- [x] **Лайки постов** - автоматически через `createNotification()`
- [x] **Комментарии** - автоматически через `createNotification()`
- [x] **Лайки комментариев** - автоматически через `createNotification()`
- [x] **Подписки** - автоматически через `createNotification()`
- [x] **Сообщения** - напрямую через `sendNotificationForEvent()`

## 📚 Документация

- [x] `FCM_SETUP.md` - инструкция по настройке Firebase для Flutter
- [x] `FCM_BACKEND_SETUP.md` - инструкция по настройке backend
- [x] `FCM_IMPLEMENTATION_CHECKLIST.md` - этот файл

## ⚠️ Важные замечания

1. **Упоминания (mentions)** - в данный момент не отправляют уведомления. Если нужно добавить:
   - Добавить создание уведомлений при сохранении в `post_mentions`
   - Интегрировать в FCM сервис

2. **Новые посты от подписок** - пока не отправляются push-уведомления (только через `/api/notifications/check`)

3. **Новые stories** - пока не отправляются push-уведомления (только через `/api/notifications/check`)

## 🚀 Готово к использованию!

После выполнения всех пунктов настройки система FCM будет полностью работоспособна.
