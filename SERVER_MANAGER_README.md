# Fuisor Server Manager

Красивый TUI (Terminal User Interface) для управления сервером Fuisor с категоризированными логами.

## Установка

Установите зависимости:
```bash
npm install
```

## Запуск

Запустите сервер с менеджером:
```bash
npm run manager
```

Или запустите сервер отдельно:
```bash
npm start
```

## Возможности

### Категории логов

- **POSTS** - Логи связанные с постами (создание, обновление, удаление)
- **AUTH** - Логи авторизации и регистрации
- **RECOMMENDATIONS** - Логи рекомендаций и feed
- **SEARCH** - Логи поиска
- **MESSAGES** - Логи сообщений
- **USERS** - Логи пользователей
- **FOLLOW** - Логи подписок
- **HASHTAGS** - Логи хештегов
- **NOTIFICATIONS** - Логи уведомлений
- **SERVER** - Логи сервера
- **ERROR** - Все ошибки
- **GENERAL** - Общие логи

### Управление

#### Навигация по категориям:
- `1` - Показать все логи
- `2` - Показать логи постов
- `3` - Показать логи авторизации
- `4` - Показать логи рекомендаций
- `5` - Показать логи поиска
- `6` - Показать логи сообщений
- `7` - Показать только ошибки

#### Действия:
- `R` - Перезагрузить сервер
- `C` - Очистить логи
- `↑↓` - Прокрутка логов
- `Q` или `ESC` - Выход

### Интерфейс

Менеджер показывает:
- **Меню** (слева) - Навигация и статус сервера
- **Статистика** (справа вверху) - Количество логов по категориям
- **Логи** (внизу) - Отфильтрованные логи с цветовой кодировкой

### Цветовая кодировка

- 🟢 Зеленый - Успешные операции
- 🔴 Красный - Ошибки
- 🟡 Желтый - Предупреждения
- 🔵 Синий - Информация
- ⚪ Серый - Общие сообщения

## Структура

```
src/
├── utils/
│   └── logger.js          # Система логирования с категориями
├── cli/
│   └── server-manager.js   # CLI менеджер с TUI
└── routes/
    └── ...                 # Роуты с интегрированным логированием
```

## Использование в коде

```javascript
import { logger } from './utils/logger.js';

// Логирование по категориям
logger.post('Post created', { postId: '123' });
logger.auth('User logged in', { userId: '456' });
logger.recommendations('Feed loaded', { postsCount: 10 });
logger.search('Search performed', { query: 'test' });

// Логирование ошибок
logger.postError('Failed to create post', error);
logger.authError('Login failed', error);
```

## Примечания

- Максимальное количество хранимых логов: 1000
- Логи автоматически обновляются в реальном времени
- Статистика обновляется каждые 2 секунды
- При перезагрузке сервера все логи сохраняются

